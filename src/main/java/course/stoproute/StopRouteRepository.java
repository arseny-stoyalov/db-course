package course.stoproute;

import course.stoproute.StopRoute;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StopRouteRepository extends JpaRepository<StopRoute, Long> {
}
